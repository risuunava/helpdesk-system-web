{
  "name": "helpdesk-system",
  "private": true,
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND,ML -c blue,green,yellow \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:ml\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && php artisan serve --host=127.0.0.1 --port=8000",
    "dev:ml": "cd ml-service && venv\\Scripts\\python app.py"
  },
  "dependencies": {
    "sonner": "^2.0.7"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}